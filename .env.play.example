# Google Play — copia in .env.play (root o mobile/; non committare).
# Play Console → Setup → API access → service account (JSON)

PLAY_SERVICE_ACCOUNT_JSON=
# internal | alpha | beta | production
PLAY_TRACK=internal
