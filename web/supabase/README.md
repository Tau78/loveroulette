# Deprecated — canonical migrations

Love Game uses the **shared MusicPro Supabase project** (`fvxdghqpavdcohczrvsc`).

Do **not** apply migrations in this folder. Use:

```
APP Eventi da GAS/musicpro-eventi-app/supabase/migrations/
```

Key migration for Love Roulette:

- `20260624160000_love_roulette_module.sql`

The local `20260619000000_initial_schema.sql` was the M1 standalone prototype and is superseded.
